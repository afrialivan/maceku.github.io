const hov = document.getElementById('hov');

function hover1(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='-15px'
}

function hover2(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='55px'
}

function hover3(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='120px'
}

function hover4(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='195px'
}

function hover5(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='265px'
}

function hover6(){
  // li.forEach(e => e.style.color = `white`)
  hov.style.top='340px'
}

function out() {
  hov.style.top='-15px'
  // li.forEach(e => e.style.color = `black`)
}